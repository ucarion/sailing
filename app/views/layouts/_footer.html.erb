<footer class="footer">
  <small>
    LJHS Sailing &mdash; <%= signed_in? ? "You are logged in as #{current_user.name}" : "You are not logged in." %>
    <%= " (admin)" if current_user && current_user.admin? %>
  </small>
  <nav>
    <ul>
      <li><%= link_to "About",   about_path %></li>
      <li><%= link_to "Contact", contact_path %></li>
    </ul>
  </nav>
</footer>