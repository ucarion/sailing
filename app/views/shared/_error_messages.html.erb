<% if @user.errors.any? %>
  <div class="alert alert-error alert-block">
    <h4>Your form contains <%= pluralize(@user.errors.count, "error") %></h4>

    <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>